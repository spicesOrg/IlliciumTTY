# AGENTS.md

## 项目工作约定

Codex 在本仓库中工作时，应优先遵守本文件中的项目级规则。

---

## 分支工作约定

所有修改都必须在 `dev` 分支中进行。

开始修改前应确认当前分支为 `dev`；如不在 `dev` 分支，应先切换到 `dev` 分支后再修改。

---

## Git 提交规范

Git commit message 规范已拆分到 `COMMIT_MESSAGE.md`。

仅当任务涉及以下内容时，才读取并遵守 `COMMIT_MESSAGE.md`：

- 生成 commit message
- 修改 commit message
- 批量整理 commit message
- 生成 changelog
- 整理 PR 描述中的提交记录
- 检查提交信息是否符合项目规范
- 代码修改完成后创建 git commit

非 Git 场景下，不要主动展开 `COMMIT_MESSAGE.md`，以避免上下文过长。

---

## 代码修改后的自动提交要求

每次完成代码修改后，必须自动创建一次 git commit。

### 执行规则

当 Codex 修改了代码、测试、脚本、构建配置或其他会影响项目行为的文件后，必须执行以下流程：

1. 使用 `git status` 检查工作区状态。
2. 使用 `git diff` 核对本次修改内容。
3. 仅暂存本次任务相关文件，不要暂存用户已有的无关改动。
4. 按照 `COMMIT_MESSAGE.md` 生成符合项目规范的中文 commit message。
5. 执行 `git commit` 完成提交。
6. 在最终回复中简要说明已提交的 commit message。

### 例外情况

以下情况不自动提交：

- 用户明确要求不要提交。
- 用户只要求分析、解释、规划或审查代码，没有要求修改代码。
- 本次任务没有产生实际文件变更。
- 仓库当前不是 Git 仓库。
- Git 提交失败且无法在当前任务中安全修复。

### 遇到未关联改动时

如果工作区中存在用户已有的未提交改动：

- 不要覆盖、回滚或格式化这些无关改动。
- 不要使用 `git add .`。
- 只暂存 Codex 本次任务修改的文件。
- 如果无法可靠区分哪些文件属于本次任务，应停止提交并在最终回复中说明原因。

### 提交信息

提交信息必须遵守 `COMMIT_MESSAGE.md` 中定义的格式：

```text
:<type>: (<scope>): <summary> [#issue]
```

示例：

```text
:bug: (ocr): 修复双击返回键触发 OCR 参数错误
:sparkles: (tap): 添加设备背部双击检测模块
:zap: (ocr): 优化前台包名检测逻辑，避免识别滞后与空值 #1367
```

---

## Codex 输出要求

当本次任务产生代码变更并已提交时，最终回复应包含：

- 修改摘要
- 已执行的验证或检查
- commit message

不要输出冗长的 git diff，除非用户明确要求。
